---
- name: check if backup exists
  stat:
    path: "{{ certbot_backup_file }}"
  register: r

- include: restore_conditional.yml
  when:
    - r.stat.exists is defined

- include: permissions.yml
  when:
    - r.stat.exists is defined
